spring:
  application:
    name: carrent

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:jdbc}

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:5Sy1-R-Zj7ajgcabswkEttARiS8Q4Vs9IhYCVAt1bQ0}
  expiration: ${JWT_EXPIRATION:3600000}

---

spring:
  config:
    activate:
      on-profile: jpa
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-hidden-grass-aqoanzvi.c-8.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_OusabvTHX7Y9&sslmode=require}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

---

spring:
  config:
    activate:
      on-profile: jdbc
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-hidden-grass-aqoanzvi.c-8.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_OusabvTHX7Y9&sslmode=require}
    driver-class-name: org.postgresql.Driver
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration